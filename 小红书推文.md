# 小红书推文草稿 — dsh-board-ui（DeepSeek Harness 看板界面）

> 定位：以「你正在做的工具」为主角 —— **DeepSeek Harness（多 Agent 任务编排系统）+
> 你手写的看板形态 UI 插件**。标题/正文可自由剪裁。

---

## 一、标题（候选 5 个，按推荐度排序）

1. 我把多 Agent 调度台变成了看板，状态一眼看穿 🔥
2. 给 DeepSeek Harness 装了个看板皮肤，子代理谁在摸鱼一目了然
3. 多智能体跑任务全靠猜？我把执行状态做成了看板
4. 谁说 AI 工具只能聊天框？我把 Agent 流水线做成了看板
5. 十几路子代理同时跑，我终于不用盯着日志发呆了

> 小红书标题建议 ≤ 20 字，数字 + 对比 + 情绪词更有点击率。

---

## 二、正文（主推版 · 偏「使用爽感」）

做了个把多 Agent 调度变成看板的工具，忍不住来分享 🫣

🎯 先说痛点
用多智能体系统跑任务的时候，十几个子代理同时在干活，它们进行到哪一步全靠猜——
翻日志、盯对话框，哪个在跑、哪个在等审批、哪个挂了，全凭感觉。

💡 所以给我的 DeepSeek Harness 写了个 UI 外壳插件，把 Web 界面整个改造成了一块大看板：

📋 任务走六状态生命周期
待执行 → 进行中 → 需要处理 → 验收中 → 已完成
每张任务卡走到哪一步，抬头就看见。卡上还有子 Agent 统计：
● 运行中 / ✓ 已完成 / ○ 等待 / ✕ 失败，谁在干活谁在摸鱼，一目了然。

🖱️ 交互细节拉满
· 拖拽排序，拖到「进行中」直接启动计划
· 右键菜单：重命名 / 复制 / 归档 / 重新执行
· j/k 键盘导航 + Enter 打开，Ctrl+K 全局命令面板
· 悬浮会话抽屉，不挤压看板，宽度还能拖
· 亮色 / 暗色双主题，窄窗口自动收成侧栏

✅ 最爽的是无痛安装
几行配置挂上，想回退删两行就恢复官方 UI，一点心理负担都没有。

如果你也在折腾多 Agent 工具链，真的建议试试把执行状态变成可见的流程，
比盯日志高效 10 倍不止。评论区欢迎交流！🙌

#AI编程 #多智能体 #Agent #效率工具 #大模型 #看板 #DeepSeek #开源

---

## 三、备选正文（偏「技术拆解」· 面向开发者）

给 DeepSeek Harness 写了个看板 UI 插件，技术细节分享一下 🛠️

多 Agent 系统最反直觉的一点：执行是并发的，可界面只有对话流。
于是我用 dsh 的客户端插件机制，直接接管了 Web 界面的 root 槽位，
把官方 UI 重构成了看板形态：

· 五列状态：待执行 / 进行中 / 需要处理 / 验收中 / 已完成，
  状态全部来自运行时的真实信号（session.running / pendingInteraction /
  completed 等），不是玩具投影
· 任务状态与 Agent 状态分离：卡片聚合子代理计数
  （● 运行中 / ✓ 完成 / ○ 等待 / ✕ 失败），失败红色告警 + 重试入口
· 列头带实时描述，如「进行中 3 · 2 个 Agent 正在运行」
· 交互：拖拽排序、拖到进行中启动计划、右键菜单、j/k 导航、
  Ctrl+K 命令面板、悬浮抽屉
· 暗色主题联动官方主题服务，对比度过了 AA
· 回退成本为零：删掉两行配置即恢复官方 UI

插件走 esbuild 构建，产物直接提交，clone 开箱即用；安装也就是
link + 两行 cordis.patch.yml。整套下来最深的感受：**状态可视化
是 Agent 工具的第一生产力**，日志流只是兜底。

源码思路和 README 都在仓库里，欢迎 star 和 PR 👀

#前端 #AI编程 #Agent #多智能体 #DeepSeek #开源 #效率工具 #程序员

---

## 四、配图建议（小红书没图没流量）

1. **封面**：看板全景截图（五列 + 彩色任务卡），叠 3-5 个大字卖点
   「多 Agent 调度 × 看板」
2. **图 2**：单张任务卡放大 —— 子 Agent 统计胶囊 + 状态色条
3. **图 3**：暗色主题 + 悬浮会话抽屉
4. **图 4**：Ctrl+K 命令面板 / 键盘导航
5. **图 5**：安装前后对比（官方 UI vs 看板 UI）

> 封面建议 3:4，加一张「安装代码截图」对程序员受众很有效。

---

## 五、发布小贴士

- **正文里可以埋一句钩子**：评论区扣「看板」发安装步骤（拉互动率）
- 发文时间：工作日 12:00-13:00 或 20:00-22:00（程序员受众活跃时段）
- 话题不要贪多，5-8 个精准 tag 即可；「#效率工具」这类泛流量 tag 留 1-2 个
- 如果重点想推**插件本身**（面向正在用 dsh 的人），把「官方 UI vs 看板」对比
  前置到第一屏；如果想推 **DSH 这个工具**，把「多 Agent 编排」的价值前置
